/** Default page sizes used across paginated views. */
export const PAGE_SIZE = {
  SM: 10,
  MD: 20,
  LG: 24,
} as const

/** Debounce delay (ms) for search input in tables. */
export const SEARCH_DEBOUNCE_MS = 300
